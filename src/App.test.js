import React from 'react';
import ReactDOM from 'react-dom';
import PowersOfTwo from './App';

it('should pass test', () => {
    // description of test
    expect(0).toEqual(0);
});

it('should fail test', () => {
    // description of test
    expect(0).toEqual(1);
});
